spring:
  datasource:
    url: jdbc:h2:mem:fixguide-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

app:
  jwt:
    secret: fixguide-test-secret-key-that-is-long-enough-for-hs256
    expires-in-seconds: 3600
    issuer: fixguide-test
  cors:
    allowed-origins: http://localhost:5173
  seed:
    enabled: false
  auth:
    token-blacklist:
      # 테스트는 Redis 없이 돈다. 환경변수가 끼어들지 않도록 값을 고정한다.
      type: memory

logging:
  level:
    com.skala.fixguide: INFO
