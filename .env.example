# 복사해서 .env 로 사용: cp .env.example .env

# Database (PostgreSQL)
DB_NAME=fixguide
DB_USER=fixguide
DB_PASSWORD=fixguide
DB_PORT=5432

# Backend (Spring Boot)
SERVER_PORT=8080

# Frontend (Vue + Vite)
FRONTEND_PORT=5173
VITE_API_BASE_URL=http://localhost:8080
